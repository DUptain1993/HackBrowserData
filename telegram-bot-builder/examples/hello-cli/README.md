# hello-cli

A tiny command-line text utility. Give it text and it reports word count,
character count, and an all-caps version.

## Usage

```
python cli.py "some text here"
python cli.py --upper "shout this"
```
